# Dropwizard-model.Games-Store

This is a small REST server that connects to a MySQL database and uses Dropwizard to create REST API to access and modify the database.
It authenticates users using JWTs. 
